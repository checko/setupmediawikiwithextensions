# Copy to .env and customize before first run
MW_SITE_NAME=MyWiki
MW_LANG=en
MW_ADMIN_USER=Admin
MW_ADMIN_PASS=changeme123!

MW_DB_NAME=wikidb
MW_DB_USER=wiki
MW_DB_PASS=wiki_pass
MW_DB_ROOT_PASSWORD=root_pass

# The public URL your wiki will use
MW_SITE_SERVER=http://localhost:9090

# Max pixel area for image thumbnails (default allows large images)
# 12_500_000 is MW default (12.5 MP). Example below sets 100 MP.
MW_MAX_IMAGE_AREA=100000000

# SVG rendering (optional)
# Maximum rasterized size for SVG thumbnails (in pixels on the largest side)
# Default is 4096; increase if you need larger SVG thumbs.
MW_SVG_MAX_SIZE=4096
# Preferred SVG converter: auto | rsvg | ImageMagick | inkscape
# "auto" prefers rsvg when available (recommended)
MW_SVG_CONVERTER=auto

# Mermaid (diagram rendering)
# Theme options: forest (default), default, neutral, dark
MW_MERMAID_THEME=forest

# ResourceLoader debug (disable JS minification; useful for troubleshooting)
# 0 = off (default), 1 = on
MW_RL_DEBUG=0

# If your uploads come as a ZIP created on a system that uses a non‑UTF‑8
# filename encoding (e.g., CP950, GBK), set this to preserve names:
# Example: MW_ZIP_ENCODING=cp950  or MW_ZIP_ENCODING=gbk
MW_ZIP_ENCODING=

# --- Optional: Auto-restore on first startup ---
# Enable one-time restore of DB dump and uploads from files in ./data
# 0 = disabled (default), 1 = enabled
MW_RESTORE_ON_INIT=0
# Path (inside container) to DB dump: supports .sql or .sql.gz
# If you place wikidb.sql in ./ (repo root), prefer moving it into ./data and set:
# MW_RESTORE_DB_DUMP=/data/wikidb.sql
MW_RESTORE_DB_DUMP=
# Path (inside container) to uploads archive: .zip, .tar.gz, or .tgz
# e.g. MW_RESTORE_UPLOADS_ARCHIVE=/data/images.zip
MW_RESTORE_UPLOADS_ARCHIVE=
# If DB already has tables and you want to overwrite with the dump, set to 1
MW_FORCE_DB_RESTORE=0
