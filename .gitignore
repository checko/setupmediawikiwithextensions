# Env and local overrides
.env

# Local MediaWiki config persisted by init script
data/

# Node/editor junk
.DS_Store
.idea/
.vscode/

# Composer cache (if used locally)
vendor/
composer.lock
# Ignore large import artifacts at repo root
/*.sql
/*.sql.gz
/images.*
/*.zip
/*.tar.gz
/*.tgz
/*.bundle
/*.patch
